import { styled } from '@mui/material/styles';
import { Container } from '@mui/material';

export const BigStyledContainer = styled(Container)(() => ({width: "1700px", "padding": "20px 0"}));
export const SmallStyledContainer = styled(Container)(() => ({width: "900px"}));